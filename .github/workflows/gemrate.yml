name: Gemrate Grading Sync

on:
  workflow_dispatch:
  schedule:
    # Run every 2 hours to process batches of 10 athletes
    - cron: '0 */2 * * *'

concurrency:
  group: gemrate-sync
  cancel-in-progress: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch Gemrate data (batch)
        run: python scripts/fetch_gemrate.py

      - name: Commit and Push changes
        run: |
          set -e

          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

          git add data/gemrate.json public/data/gemrate.json data/gemrate-progress.json || true

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Automated sync: Updated PSA grading data (batch)"

          git fetch origin main
          git rebase origin/main || (git rebase --abort && git pull --rebase origin main)

          git push origin HEAD:main
