name: Update eBay sold averages (batched)

on:
  schedule:
    # Every 3 hours starting at 00:00 UTC — staggered 90min from graded-sold
    - cron: "0 0,3,6,9,12,15,18,21 * * *"
  workflow_dispatch:
    inputs:
      only:
        description: "Single athlete name (e.g. Ronald Acuna Jr.) — leave empty for full batch"
        required: false
        default: ""

concurrency:
  group: ebay-sold-avg-main
  cancel-in-progress: true

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install deps
        if: hashFiles('package-lock.json') != ''
        run: npm install --no-audit --no-fund

      - name: Update eBay sold averages (batch of 10)
        run: node scripts/sold-update-ebay-avg.js
        env:
          EBAY_ONLY: ${{ github.event.inputs.only }}

      - name: Copy to public
        run: |
          mkdir -p public/data
          cp data/ebay-sold-avg.json public/data/ebay-sold-avg.json 2>/dev/null || true

      - name: Commit & push (rebase-safe)
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data/ebay-sold-avg.json data/ebay-sold-progress.json public/data/ebay-sold-avg.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Update eBay sold averages (batch)"

          git fetch origin main
          git rebase --autostash origin/main || (git rebase --abort && git pull --rebase origin main)

          git push origin HEAD:main
